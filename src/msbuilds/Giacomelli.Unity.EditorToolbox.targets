 <Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003"> 
      <Target Name="AfterBuild">
          <ItemGroup> 
              <FilesToCopy Include="$(MSBuildProjectDirectory)\bin\$(Configuration)\**\*" Exclude="$(MSBuildProjectDirectory)\bin\$(Configuration)\Unity*.dll" />
          </ItemGroup>
 	
          <Message Text="### Copying $(MSBuildProjectName) files ..." Importance="high" />
 
          <Copy
              SourceFiles="@(FilesToCopy)" DestinationFiles="@(FilesToCopy->'$(MSBuildProjectDirectory)\..\..\..\..\..\..\Skahal\Apps\Buildron-Classic-Mods\src\Buildron.ClassicMods.BuildMod\Assets\Scripts\references\%(RecursiveDir)%(Filename)%(Extension)')"/>         
    </Target>
</Project>